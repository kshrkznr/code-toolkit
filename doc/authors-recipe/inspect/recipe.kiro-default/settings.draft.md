# Settings View

## Inventory

### runtime.draft.settings.json

#### [bat]

```diff
+ ["[bat]", "editor.insertSpaces"]=true
+ ["[bat]", "files.encoding"]="shiftjis"
+ ["[bat]", "files.eol"]="\r\n"
+ ["[bat]"]={}
```

#### [csv]

```diff
+ ["[csv]", "editor.minimap.enabled"]=false
+ ["[csv]", "editor.rulers", [0]]=120
+ ["[csv]", "editor.rulers"]=[]
+ ["[csv]", "editor.wordWrap"]="off"
+ ["[csv]"]={}
```

#### [git-commit]

```diff
+ ["[git-commit]", "editor.wordWrap"]="bounded"
+ ["[git-commit]"]={}
```

#### [ini]

```diff
+ ["[ini]", "editor.insertSpaces"]=true
+ ["[ini]"]={}
```

#### [json]

```diff
+ ["[json]", "editor.suggest.insertMode"]="replace"
+ ["[json]"]={}
```

#### [markdown]

```diff
+ ["[markdown]", "editor.tabSize"]=2
+ ["[markdown]", "files.trimTrailingWhitespace"]=false
+ ["[markdown]"]={}
```

#### [plaintext]

```diff
+ ["[plaintext]", "files.trimTrailingWhitespace"]=false
+ ["[plaintext]"]={}
```

#### [powershell]

```diff
+ ["[powershell]", "editor.insertSpaces"]=true
+ ["[powershell]", "files.encoding"]="shiftjis"
+ ["[powershell]", "files.eol"]="\r\n"
+ ["[powershell]"]={}
```

#### [prompt]

```diff
+ ["[prompt]", "editor.insertSpaces"]=true
+ ["[prompt]", "files.encoding"]="shiftjis"
+ ["[prompt]", "files.eol"]="\r\n"
+ ["[prompt]"]={}
```

#### [properties]

```diff
+ ["[properties]", "editor.insertSpaces"]=false
+ ["[properties]"]={}
```

#### [shellscript]

```diff
+ ["[shellscript]", "editor.insertSpaces"]=true
+ ["[shellscript]"]={}
```

#### [textile]

```diff
+ ["[textile]", "editor.insertSpaces"]=true
+ ["[textile]", "editor.rulers", [0]]=120
+ ["[textile]", "editor.rulers", [1]]=160
+ ["[textile]", "editor.rulers"]=[]
+ ["[textile]", "editor.wordWrap"]="bounded"
+ ["[textile]"]={}
```

#### [toml]

```diff
+ ["[toml]", "editor.defaultFormatter"]="tamasfe.even-better-toml"
+ ["[toml]", "editor.foldingStrategy"]="indentation"
+ ["[toml]", "editor.insertSpaces"]=true
+ ["[toml]", "editor.suggest.insertMode"]="replace"
+ ["[toml]"]={}
```

#### [tsv]

```diff
+ ["[tsv]", "editor.minimap.enabled"]=false
+ ["[tsv]", "editor.rulers", [0]]=120
+ ["[tsv]", "editor.rulers"]=[]
+ ["[tsv]", "editor.wordWrap"]="off"
+ ["[tsv]"]={}
```

#### [xml]

```diff
+ ["[xml]", "editor.defaultFormatter"]="redhat.vscode-xml"
+ ["[xml]", "editor.insertSpaces"]=true
+ ["[xml]", "editor.suggest.insertMode"]="replace"
+ ["[xml]"]={}
```

#### [yaml]

```diff
+ ["[yaml]", "editor.defaultFormatter"]="redhat.vscode-yaml"
+ ["[yaml]", "editor.foldingStrategy"]="indentation"
+ ["[yaml]", "editor.insertSpaces"]=true
+ ["[yaml]", "editor.suggest.insertMode"]="replace"
+ ["[yaml]"]={}
```

#### asciidoc.preview.refreshInterval

```diff
+ ["asciidoc.preview.refreshInterval"]=500
```

#### asciidoc.preview.useEditorStyle

```diff
+ ["asciidoc.preview.useEditorStyle"]=true
```

#### code-eol.crlfCharacter

```diff
+ ["code-eol.crlfCharacter"]="↵"
```

#### code-eol.crlfCharacterStyle

```diff
+ ["code-eol.crlfCharacterStyle", "color"]="#d1d41b"
+ ["code-eol.crlfCharacterStyle", "opacity"]=0.8
+ ["code-eol.crlfCharacterStyle"]={}
```

#### code-eol.newlineCharacter

```diff
+ ["code-eol.newlineCharacter"]="↓"
```

#### code-eol.newlineCharacterStyle

```diff
+ ["code-eol.newlineCharacterStyle", "color"]="#d1d41b"
+ ["code-eol.newlineCharacterStyle", "opacity"]=0.8
+ ["code-eol.newlineCharacterStyle"]={}
```

#### code-eol.returnCharacter

```diff
+ ["code-eol.returnCharacter"]="←"
```

#### code-eol.returnCharacterStyle

```diff
+ ["code-eol.returnCharacterStyle", "color"]="#d1d41b"
+ ["code-eol.returnCharacterStyle", "opacity"]=0.8
+ ["code-eol.returnCharacterStyle"]={}
```

#### code-eol.style

```diff
+ ["code-eol.style", "color"]="#d1d41b"
+ ["code-eol.style", "opacity"]=1
+ ["code-eol.style"]={}
```

#### code-eol.toggleWithWhiteSpace

```diff
+ ["code-eol.toggleWithWhiteSpace"]=true
```

#### editor.colorDecorators

```diff
+ ["editor.colorDecorators"]=false
```

#### editor.copyWithSyntaxHighlighting

```diff
+ ["editor.copyWithSyntaxHighlighting"]=false
```

#### editor.cursorSmoothCaretAnimation

```diff
+ ["editor.cursorSmoothCaretAnimation"]="off"
```

#### editor.defaultColorDecorators

```diff
+ ["editor.defaultColorDecorators"]="never"
```

#### editor.fontSize

```diff
+ ["editor.fontSize"]=13
```

#### editor.formatOnPaste

```diff
+ ["editor.formatOnPaste"]=false
```

#### editor.formatOnSave

```diff
+ ["editor.formatOnSave"]=false
```

#### editor.formatOnType

```diff
+ ["editor.formatOnType"]=true
```

#### editor.guides.bracketPairs

```diff
+ ["editor.guides.bracketPairs"]=true
```

#### editor.guides.bracketPairsHorizontal

```diff
+ ["editor.guides.bracketPairsHorizontal"]=false
```

#### editor.guides.highlightActiveBracketPair

```diff
+ ["editor.guides.highlightActiveBracketPair"]=true
```

#### editor.insertSpaces

```diff
+ ["editor.insertSpaces"]=true
```

#### editor.largeFileOptimizations

```diff
+ ["editor.largeFileOptimizations"]=true
```

#### editor.minimap.enabled

```diff
+ ["editor.minimap.enabled"]=false
```

#### editor.multiCursorModifier

```diff
+ ["editor.multiCursorModifier"]="ctrlCmd"
```

#### editor.renderControlCharacters

```diff
+ ["editor.renderControlCharacters"]=true
```

#### editor.renderWhitespace

```diff
+ ["editor.renderWhitespace"]="all"
```

#### editor.smoothScrolling

```diff
+ ["editor.smoothScrolling"]=false
```

#### editor.stickyScroll.enabled

```diff
+ ["editor.stickyScroll.enabled"]=false
```

#### editor.tokenColorCustomizations

```diff
+ ["editor.tokenColorCustomizations", "textMateRules", [0], "scope"]="entity.name.tag.heading.textile"
+ ["editor.tokenColorCustomizations", "textMateRules", [0], "settings", "fontStyle"]="bold"
+ ["editor.tokenColorCustomizations", "textMateRules", [0], "settings"]={}
+ ["editor.tokenColorCustomizations", "textMateRules", [0]]={}
+ ["editor.tokenColorCustomizations", "textMateRules"]=[]
+ ["editor.tokenColorCustomizations"]={}
```

#### editor.unicodeHighlight.ambiguousCharacters

```diff
+ ["editor.unicodeHighlight.ambiguousCharacters"]=true
```

#### editor.unicodeHighlight.invisibleCharacters

```diff
+ ["editor.unicodeHighlight.invisibleCharacters"]=true
```

#### editor.wordWrap

```diff
+ ["editor.wordWrap"]="off"
```

#### errorLens.enabledDiagnosticLevels

```diff
+ ["errorLens.enabledDiagnosticLevels", [0]]="warning"
+ ["errorLens.enabledDiagnosticLevels", [1]]="error"
+ ["errorLens.enabledDiagnosticLevels"]=[]
```

#### errorLens.fontStyleItalic

```diff
+ ["errorLens.fontStyleItalic"]=false
```

#### errorLens.gutterIconsEnabled

```diff
+ ["errorLens.gutterIconsEnabled"]=true
```

#### errorLens.messageBackgroundMode

```diff
+ ["errorLens.messageBackgroundMode"]="none"
```

#### errorLens.messageEnabled

```diff
+ ["errorLens.messageEnabled"]=true
```

#### errorLens.messageMaxChars

```diff
+ ["errorLens.messageMaxChars"]=120
```

#### evenBetterToml.formatter.columnWidth

```diff
+ ["evenBetterToml.formatter.columnWidth"]=120
```

#### explorer.autoReveal

```diff
+ ["explorer.autoReveal"]=false
```

#### explorer.compactFolders

```diff
+ ["explorer.compactFolders"]=false
```

#### explorer.confirmDelete

```diff
+ ["explorer.confirmDelete"]=true
```

#### explorer.confirmDragAndDrop

```diff
+ ["explorer.confirmDragAndDrop"]=true
```

#### explorer.decorations.badges

```diff
+ ["explorer.decorations.badges"]=false
```

#### explorer.decorations.colors

```diff
+ ["explorer.decorations.colors"]=true
```

#### explorer.openEditors.visible

```diff
+ ["explorer.openEditors.visible"]=5
```

#### explorer.sortOrder

```diff
+ ["explorer.sortOrder"]="default"
```

#### extensions.ignoreRecommendations

```diff
+ ["extensions.ignoreRecommendations"]=true
```

#### files.associations

```diff
+ ["files.associations", "*.conf"]="ini"
+ ["files.associations", "*.draft"]="markdown"
+ ["files.associations", "*.env"]="properties"
+ ["files.associations", "*.limy"]="properties"
+ ["files.associations", "*.redmine"]="textile"
+ ["files.associations", "*.textile"]="textile"
+ ["files.associations"]={}
```

#### files.autoSave

```diff
+ ["files.autoSave"]="off"
```

#### files.encoding

```diff
+ ["files.encoding"]="utf8"
```

#### files.eol

```diff
+ ["files.eol"]="\n"
```

#### files.hotExit

```diff
+ ["files.hotExit"]="onExitAndWindowClose"
```

#### files.insertFinalNewline

```diff
+ ["files.insertFinalNewline"]=true
```

#### files.trimTrailingWhitespace

```diff
+ ["files.trimTrailingWhitespace"]=true
```

#### git-graph.openNewTabEditorGroup

```diff
+ ["git-graph.openNewTabEditorGroup"]="Beside"
```

#### git-graph.repository.showUncommittedChanges

```diff
+ ["git-graph.repository.showUncommittedChanges"]=false
```

#### git-graph.repository.showUntrackedFiles

```diff
+ ["git-graph.repository.showUntrackedFiles"]=false
```

#### git-graph.retainContextWhenHidden

```diff
+ ["git-graph.retainContextWhenHidden"]=false
```

#### git.autofetch

```diff
+ ["git.autofetch"]=false
```

#### git.confirmSync

```diff
+ ["git.confirmSync"]=false
```

#### git.enableSmartCommit

```diff
+ ["git.enableSmartCommit"]=false
```

#### git.openRepositoryInParentFolders

```diff
+ ["git.openRepositoryInParentFolders"]="always"
```

#### github.copilot.enable

```diff
+ ["github.copilot.enable", "*"]=false
+ ["github.copilot.enable"]={}
```

#### gitlens.blame.compact

```diff
+ ["gitlens.blame.compact"]=true
```

#### gitlens.codeLens.enabled

```diff
+ ["gitlens.codeLens.enabled"]=false
```

#### gitlens.currentLine.enabled

```diff
+ ["gitlens.currentLine.enabled"]=true
```

#### gitlens.currentLine.format

```diff
+ ["gitlens.currentLine.format"]="${author}, ${date} • ${message}"
```

#### gitlens.currentLine.scrollable

```diff
+ ["gitlens.currentLine.scrollable"]=false
```

#### gitlens.graph.minimap.enabled

```diff
+ ["gitlens.graph.minimap.enabled"]=false
```

#### gitlens.heatmap.locations

```diff
+ ["gitlens.heatmap.locations"]=[]
```

#### gitlens.hovers.enabled

```diff
+ ["gitlens.hovers.enabled"]=true
```

#### gremlins.characters

```diff
+ ["gremlins.characters", "00a0", "description"]="nbsp"
+ ["gremlins.characters", "00a0", "level"]="warning"
+ ["gremlins.characters", "00a0"]={}
+ ["gremlins.characters", "200b", "description"]="Zero Width Space"
+ ["gremlins.characters", "200b", "level"]="warning"
+ ["gremlins.characters", "200b", "zeroWidth"]=true
+ ["gremlins.characters", "200b"]={}
+ ["gremlins.characters", "3000", "description"]="Full Wide Space"
+ ["gremlins.characters", "3000", "level"]="warning"
+ ["gremlins.characters", "3000"]={}
+ ["gremlins.characters"]={}
```

#### gremlins.color_warning

```diff
+ ["gremlins.color_warning"]="rgba(255, 180, 80, .20)"
```

#### gremlins.gutterIconSize

```diff
+ ["gremlins.gutterIconSize"]="0%"
```

#### gremlins.showInProblemPane

```diff
+ ["gremlins.showInProblemPane"]=false
```

#### indentRainbow.colors

```diff
+ ["indentRainbow.colors", [0]]="rgba(255,255,255,0.03)"
+ ["indentRainbow.colors", [1]]="rgba(255,255,255,0.06)"
+ ["indentRainbow.colors", [2]]="rgba(255,255,255,0.09)"
+ ["indentRainbow.colors", [3]]="rgba(255,255,255,0.12)"
+ ["indentRainbow.colors"]=[]
```

#### indentRainbow.errorColor

```diff
+ ["indentRainbow.errorColor"]="rgba(255,255,255,0.0)"
```

#### indentRainbow.indicatorStyle

```diff
+ ["indentRainbow.indicatorStyle"]="light"
```

#### indentRainbow.lightIndicatorStyleLineWidth

```diff
+ ["indentRainbow.lightIndicatorStyleLineWidth"]=1
```

#### json.format.keepLines

```diff
+ ["json.format.keepLines"]=false
```

#### markdown.extension.list.indentationSize

```diff
+ ["markdown.extension.list.indentationSize"]="adaptive"
```

#### markdown.extension.tableFormatter.enabled

```diff
+ ["markdown.extension.tableFormatter.enabled"]=true
```

#### markdown.extension.toc.updateOnSave

```diff
+ ["markdown.extension.toc.updateOnSave"]=true
```

#### markdown.preview.scrollEditorWithPreview

```diff
+ ["markdown.preview.scrollEditorWithPreview"]=true
```

#### markdown.preview.scrollPreviewWithEditor

```diff
+ ["markdown.preview.scrollPreviewWithEditor"]=true
```

#### markdownlint.config

```diff
+ ["markdownlint.config", "MD013"]=false
+ ["markdownlint.config", "MD022"]=false
+ ["markdownlint.config", "MD024"]=false
+ ["markdownlint.config", "MD025"]=false
+ ["markdownlint.config", "MD033"]=false
+ ["markdownlint.config", "MD036"]=false
+ ["markdownlint.config"]={}
```

#### markdownlint.run

```diff
+ ["markdownlint.run"]="onType"
```

#### native-ascii-converter.auto-conversion-on-activate

```diff
+ ["native-ascii-converter.auto-conversion-on-activate"]=true
```

#### native-ascii-converter.comment-conversion

```diff
+ ["native-ascii-converter.comment-conversion"]=false
```

#### native-ascii-converter.use-files.associations

```diff
+ ["native-ascii-converter.use-files.associations"]=true
```

#### partialDiff.commandsOnContextMenu

```diff
+ ["partialDiff.commandsOnContextMenu", "diffSelectionWithClipboard"]=false
+ ["partialDiff.commandsOnContextMenu", "diffVisibleEditors"]=false
+ ["partialDiff.commandsOnContextMenu", "markSection1"]=true
+ ["partialDiff.commandsOnContextMenu", "markSection2AndTakeDiff"]=true
+ ["partialDiff.commandsOnContextMenu", "togglePreComparisonTextNormalizationRules"]=false
+ ["partialDiff.commandsOnContextMenu"]={}
```

#### path-intellisense.autoSlashAfterDirectory

```diff
+ ["path-intellisense.autoSlashAfterDirectory"]=true
```

#### path-intellisense.extensionOnImport

```diff
+ ["path-intellisense.extensionOnImport"]=false
```

#### path-intellisense.mappings

```diff
+ ["path-intellisense.mappings", "~"]="${env:HOME}"
+ ["path-intellisense.mappings"]={}
```

#### rainbow_csv.comment_prefix

```diff
+ ["rainbow_csv.comment_prefix"]="#"
```

#### rainbow_csv.enable_auto_csv_lint

```diff
+ ["rainbow_csv.enable_auto_csv_lint"]=false
```

#### regex-previewer.enableCodeLens

```diff
+ ["regex-previewer.enableCodeLens"]=false
```

#### remote.SSH.useExecServer

```diff
+ ["remote.SSH.useExecServer"]=false
```

#### scm.diffDecorations

```diff
+ ["scm.diffDecorations"]="gutter"
```

#### security.workspace.trust.untrustedFiles

```diff
+ ["security.workspace.trust.untrustedFiles"]="open"
```

#### shellcheck.enable

```diff
+ ["shellcheck.enable"]=true
```

#### shellcheck.enableQuickFix

```diff
+ ["shellcheck.enableQuickFix"]=true
```

#### shellcheck.exclude

```diff
+ ["shellcheck.exclude", [0]]="SC2148"
+ ["shellcheck.exclude"]=[]
```

#### shellcheck.run

```diff
+ ["shellcheck.run"]="onSave"
```

#### telemetry.telemetryLevel

```diff
+ ["telemetry.telemetryLevel"]="crash"
```

#### terminal.integrated.copyOnSelection

```diff
+ ["terminal.integrated.copyOnSelection"]=true
```

#### terminal.integrated.cursorBlinking

```diff
+ ["terminal.integrated.cursorBlinking"]=false
```

#### terminal.integrated.cursorStyle

```diff
+ ["terminal.integrated.cursorStyle"]="line"
```

#### terminal.integrated.enableMultiLinePasteWarning

```diff
+ ["terminal.integrated.enableMultiLinePasteWarning"]="always"
```

#### terminal.integrated.fontSize

```diff
+ ["terminal.integrated.fontSize"]=13
```

#### terminal.integrated.localEchoEnabled

```diff
+ ["terminal.integrated.localEchoEnabled"]="on"
```

#### terminal.integrated.rightClickBehavior

```diff
+ ["terminal.integrated.rightClickBehavior"]="paste"
```

#### terminal.integrated.scrollback

```diff
+ ["terminal.integrated.scrollback"]=5000
```

#### terminal.integrated.smoothScrolling

```diff
+ ["terminal.integrated.smoothScrolling"]=false
```

#### terminal.integrated.stickyScroll.enabled

```diff
+ ["terminal.integrated.stickyScroll.enabled"]=true
```

#### terminal.integrated.tabs.enabled

```diff
+ ["terminal.integrated.tabs.enabled"]=false
```

#### trailing-spaces.trimOnSave

```diff
+ ["trailing-spaces.trimOnSave"]=false
```

#### window.nativeTabs

```diff
+ ["window.nativeTabs"]=false
```

#### window.newWindowProfile

```diff
+ ["window.newWindowProfile"]="core"
```

#### window.openFilesInNewWindow

```diff
+ ["window.openFilesInNewWindow"]="off"
```

#### window.openFoldersInNewWindow

```diff
+ ["window.openFoldersInNewWindow"]="default"
```

#### window.restoreWindows

```diff
+ ["window.restoreWindows"]="none"
```

#### window.titleBarStyle

```diff
+ ["window.titleBarStyle"]="custom"
```

#### workbench.colorCustomizations

```diff
+ ["workbench.colorCustomizations", "editorIndentGuide.activeBackground1"]="#6E7684"
+ ["workbench.colorCustomizations", "editorIndentGuide.background1"]="#353B45"
+ ["workbench.colorCustomizations", "editorWhitespace.foreground"]="#68707D"
+ ["workbench.colorCustomizations"]={}
```

#### workbench.colorTheme

```diff
+ ["workbench.colorTheme"]="Kiro Dark"
```

#### workbench.editor.defaultBinaryEditor

```diff
+ ["workbench.editor.defaultBinaryEditor"]="hexEditor.hexedit"
```

#### workbench.editor.empty.hint

```diff
+ ["workbench.editor.empty.hint"]="hidden"
```

#### workbench.editor.enablePreview

```diff
+ ["workbench.editor.enablePreview"]=false
```

#### workbench.editor.openSideBySideDirection

```diff
+ ["workbench.editor.openSideBySideDirection"]="right"
```

#### workbench.editorAssociations

```diff
+ ["workbench.editorAssociations"]={}
```

#### workbench.startupEditor

```diff
+ ["workbench.startupEditor"]="none"
```

#### workbench.statusBar.visible

```diff
+ ["workbench.statusBar.visible"]=true
```

#### xml.format.maxLineWidth

```diff
+ ["xml.format.maxLineWidth"]=120
```

#### xml.format.preserveEmptyContent

```diff
+ ["xml.format.preserveEmptyContent"]=true
```

#### xml.format.splitAttributes

```diff
+ ["xml.format.splitAttributes"]="preserve"
```

#### yaml.format.printWidth

```diff
+ ["yaml.format.printWidth"]=120
```

#### yaml.hover

```diff
+ ["yaml.hover"]=true
```

#### yaml.keyOrdering

```diff
+ ["yaml.keyOrdering"]=false
```
