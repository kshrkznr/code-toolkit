name: vscode-golang
os: macos
platform: code

runtime:
  - theme
  - golang

profile:
  - core
  - inspect
  - ops
  - review
  - struct

config:
  dist-strategy:
    default-profile:
      extensions: clean
      mcp: runtime
