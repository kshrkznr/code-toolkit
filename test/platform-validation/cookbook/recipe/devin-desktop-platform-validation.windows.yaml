name: devin-validation-windows
os: windows
platform: devin-desktop

runtime:
  - validation

profile:
  - validation

config:
  dist-strategy:
    extension-marketplace: true
    extension-pool: refresh
    lock-mode: refresh
    default-profile:
      settings: runtime
      extensions: clean
      mcp: unmanaged
  profile-strategy:
    validation:
      settings: profile
